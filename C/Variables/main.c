#include <stdio.h>
#include <stdlib.h>

int main()
{
    int ano_nascimento = 1996;
    int ano_atual = 2020;

    int resultado = (ano_atual - ano_nascimento);

    printf("Jonathan is %d years old",resultado);

    return 0;
}
