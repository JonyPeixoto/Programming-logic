#include <stdio.h>
#include <stdlib.h>

int main()
{
    int cinema_time = 20;
    int current_time = 20;

    // Can I open in the cinema ?
    if(cinema_time == current_time){
        printf("I can open! The time is the same");
    }

    return 0;
}
